type RoomType {
    id: ID
    name: String
    description: String
}

type RoomTypePageResponseType {
    count: Int
    rows: [RoomType]
}

input RoomTypeFilterInput {
    id: ID
    name: String
    pagination: Pagination
}